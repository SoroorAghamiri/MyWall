@model IEnumerable<MyWebApplication.Models.Project>
@{
    ViewData["Title"] = "My Software Projects";
    ViewBag.Title = "My Projects";
}

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/projects.css" asp-append-version="true" />
</head>

<body>
    <p class="buttonText">Software projects</p>
    <div class="project-container" id="projectContainer">
        @if (ViewData["Projects"] != null)
        {
            var projects = ViewData["Projects"] as List<Project>;
            foreach (var prj in projects)
            {
                <tr>
                    <a href='@prj.GetUrl()' style="position:relative; left:540px;">
                        <button class="projectBtn">@prj.GetName()</button>
                    </a>
                </tr>
            }
        }
        else
        {
            <p>No projects available</p>
        }
    </div>

   @* <script>
        const projectContainer = document.getElementById('projectContainer');

        let prevScrollTop = projectContainer.scrollTop;

        projectContainer.addEventListener('scroll', function() {
            if (projectContainer.scrollTop > prevScrollTop) {
                // When scrolling down, move the scroll position up by 574px
                projectContainer.scrollTop -= 30;
            }
            prevScrollTop = projectContainer.scrollTop;
        });
    </script> *@
</body>

